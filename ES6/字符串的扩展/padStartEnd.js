
    //padStart:用于头部补全
    //接受两个参数，第一个参数，一共需要补全成多少为，第二个参数，用什么补全

    //padEnd:用于尾部补全
    //接受两个参数，第一个参数，一共需要补全成多少为，第二个参数，用什么补全
    console.log('x'.padStart(4,'ab')); //abax
    console.log('x'.padEnd(5,'abd')); //xabda


